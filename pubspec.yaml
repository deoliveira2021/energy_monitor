name: energymonitor
description: "Aplicativo para monitorar consumo de energia residencial"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_switch: ^0.3.2
  intl: ^0.20.2
  mqtt_client: ^10.11.1
  sqflite: ^2.4.2
  syncfusion_flutter_charts: ^31.2.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true